# sway dotfiles

## deps

```bash
sudo pacman -S sway swaybg swayidle swaylock xorg-xwayland xorg-xrdb grim slurp brightnessctl libpulse ttf-jetbrains-mono-nerd-font wmenu foot
export PATH="$PATH:$HOME/.local/bin"
```
